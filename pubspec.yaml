name: fcli
description: A CLI tool for generating Flutter projects with Clean Architecture feature-first structure.
version: 1.0.0
homepage: https://github.com/your-username/fcli
repository: https://github.com/your-username/fcli

environment:
  sdk: ^3.0.0

dependencies:
  args: ^2.4.2
  yaml: ^3.1.2
  path: ^1.9.0
  recase: ^4.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

executables:
  fcli: fcli
